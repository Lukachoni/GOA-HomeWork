
            
            let number = prompt("შეიყვანეთ რიცხვი:");
            number = Number(number);

           
            if (number % 2 === 0) {
                alert("რიცხვი ლუწია (" + number + ")");
            } else {
                alert("რიცხვი კენტია (" + number + ")");
            }
       